<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.contabo.keycloak.spi.realmresourceprovider.browseresssion</groupId>
  <artifactId>keycloak-spi-browser-session-api</artifactId>
  <version>3.0</version>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <packaging>jar</packaging>

  <properties>
    <keycloak.version>26.7.3</keycloak.version>
    <!-- Keycloak 22 migrated from javax.* to the Jakarta EE namespace, and Keycloak 25
         replaced RESTEasy Classic with RESTEasy Reactive. `@NoCache` therefore lives in
         org.jboss.resteasy.reactive now, shipped by the Quarkus version Keycloak builds
         against - see <quarkus.version> in the keycloak-parent pom of this release. -->
    <quarkus.version>3.33.3.1</quarkus.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <!-- Keycloak 26 requires Java 17 and is itself compiled with release 17.
         Java 21 is supported at runtime, so 17 bytecode loads on every supported JVM. -->
    <java.version>17</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-core</artifactId>
      <scope>provided</scope>
      <version>${keycloak.version}</version>
    </dependency>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-server-spi</artifactId>
      <scope>provided</scope>
      <version>${keycloak.version}</version>
    </dependency>
    <!-- carries org.keycloak.services.resource.RealmResourceProvider, which moved here
         out of keycloak-services in Keycloak 26 -->
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-server-spi-private</artifactId>
      <scope>provided</scope>
      <version>${keycloak.version}</version>
    </dependency>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-services</artifactId>
      <scope>provided</scope>
      <version>${keycloak.version}</version>
    </dependency>
    <!-- provides org.jboss.resteasy.reactive.NoCache; keycloak-services declares this
         one in provided scope, and provided-scope dependencies are not transitive -->
    <dependency>
      <groupId>io.quarkus.resteasy.reactive</groupId>
      <artifactId>resteasy-reactive-common</artifactId>
      <scope>provided</scope>
      <version>${quarkus.version}</version>
    </dependency>
  </dependencies>
</project>
